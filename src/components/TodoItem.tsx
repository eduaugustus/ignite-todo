interface TodoItemProps {
	id: string;
	description: string;
	isCompleted: boolean;
}

export function TodoItem({ id, description, isCompleted }: TodoItemProps) {
	return (
		<div>
			aaa
		</div>
	)
}